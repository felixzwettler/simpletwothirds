<!DOCTYPE html>
<html>

{# ### MACROS ### #}
{% macro tree(parent, current_page_id) %}
    {% import _self as utils %}
    
    {% for child in parent %}
        
    {% endfor %}
{% endmacro %}

{% import _self as utils %}
{# ### Layout ### #}
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0"> 
    <title>
        {% if meta.title %}{{ meta.title }} | {% endif %}{{ site_title }}
    </title>
    {% if meta.description %}
        <meta name="description" content="{{ meta.description|striptags }}">
    {% endif %}
    {% if meta.robots %}
        <meta name="robots" content="{{ meta.robots }}">
    {% endif %}
    <link rel="stylesheet" href="{{ theme_url }}/css/style.css" type="text/css">
</head>

<body>
    <div class="header">
        <a href="{{ current_page.id | link }}">
            {% if current_page.title %} {{ current_page.title }}
            {% else %} {{ current_page.id }}
            {% endif %}
        </a>
    </div>
    
    <nav class="navbar">
        <ul>
            <li>
                <h3>
                    <a href="{{ base_url }}">{{ site_title }}</a>
                </h3>
            </li>
            
            {% for child in pages(depth=null, depthOffset=-1) %}
            <li>
                <a href="{{ child.url }}" {% if child.id == current_page.id %} class="active" {% else %} class="inactive" {% endif %}>
                    {{ child.id }}
                    {#{% if child.title %} {{ child.title }} {% else %} {{ child.id }} {% endif %}#}
                </a>
            </li>
            {% endfor %}
        </ul>
    </nav>
    <div class="content">
        {{ content }}
        <div class="footer">
            Follow me on Github: <a href="https://github.com/fpunktz/">fpunktz</a></br>
            This website was made with <a href="http://picocms.org/">PicoCMS</a>. The theme can be found here: <a href="https://github.com/fpunktz/simpletwothirds">github.com/fpunktz/simpletwothirds</a>.
        </div>
    </div>
</body>
</html>